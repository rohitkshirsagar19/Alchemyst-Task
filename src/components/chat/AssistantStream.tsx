export function AssistantStream() {
  return (
    <article className="card">
      <p className="card__label">Assistant stream</p>
      <p className="card__body">
        Streaming output will render here in Phase 2 once the protocol client is wired up.
      </p>
    </article>
  );
}
