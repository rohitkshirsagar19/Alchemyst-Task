export function JsonTree() {
  return (
    <article className="card">
      <p className="card__label">JSON tree</p>
      <p className="card__body">Large snapshot rendering will be added once context events are wired in.</p>
    </article>
  );
}
